{
  "extends": ["stylelint-config-recess-order", "stylelint-config-standard"],
  "rules": {
    "selector-class-pattern": [
      "^([a-z]+-*)+(__([a-z]+-*)+)?(_([a-z]+-*)+)?$",
      {
        "message": "Expected BEM (Block Element Modifier) naming convention."
      }
    ]
  }
}
